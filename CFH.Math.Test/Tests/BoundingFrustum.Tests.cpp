#include "..\HelperMethods.h"
#include "BoundingFrustum.h"

TEST(BoundingFrustum, Constructors)
{
	EXPECT_TRUE(false) << "No tests written!";
}

TEST(BoundingFrustum, Operators)
{
	EXPECT_TRUE(false) << "No tests written!";
}

TEST(BoundingFrustum, Getters_Setters)
{
	EXPECT_TRUE(false) << "No tests written!";
}

TEST(BoundingFrustum, Contains)
{
	EXPECT_TRUE(false) << "No tests written!";
}

TEST(BoundingFrustum, Intersects)
{
	EXPECT_TRUE(false) << "No tests written!";
}