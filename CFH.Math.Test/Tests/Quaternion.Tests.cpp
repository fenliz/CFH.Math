#include "..\HelperMethods.h"

TEST(Quaternion, Constructors)
{
	EXPECT_TRUE(false) << "No tests written!";
}

TEST(Quaternion, Operators)
{
	EXPECT_TRUE(false) << "No tests written!";
}

TEST(Quaternion, VectorGetters)
{
	EXPECT_TRUE(false) << "No tests written!";
}

TEST(Quaternion, Arithmetics)
{
	EXPECT_TRUE(false) << "No tests written!";
}

TEST(Quaternion, Specials)
{
	EXPECT_TRUE(false) << "No tests written!";
}